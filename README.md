# fancy-storage
A BaaS service makes offline-first storage and cross-platform synchronization with so silky-smooth API that you totally don't need to worry about cloud and synchronization.

# Key Features

- 🧠 **Silky-smooth Synchronization**: Automatically syncs with cloud data after local state changes.
- ⚡ **Seamless Use Experience**: Manage local states only and let the package handle the rest.
- 🗂️ **Schema Support**: Define data schemas to ensure consistency and type hinting in your IDEs.
- 📱 **Offline First**: Fully supports offline operations, automatically syncing when the network is restored.
- 🌐 **Multi-Platform Support**: Supports iOS / Android / macOS / Web.
- 🔒 **Secure Data Isolation**: Each sync_id is managed independently to ensure privacy and security.

## Installation

To install the `fancy_storage` package, add the following dependency to your `pubspec.yaml` file:

```yaml
dependencies:
  fancy_storage: ^0.1.0
```

Alternatively, you can install it via the command line using the following command:

```bash
flutter pub add fancy_storage
```

# Usage

Fancy Storage has a so smooth learning curve that you can use it in a matter of minutes literally.

By understanding its basic concepts and with a little previous experience in Flutter(have used any state management framework like `provider`, `riverpod` or `bloc`), you can easily get started with this package.

## Basic Concepts
There are 3 basic concepts you need to know to be a master of Fancy Storage(Yes, not an amateur but a master within several minutes only).

They are:
1. 3 levels of storage.
2. Schemas and how to define them.
3. `api_key` and `sync_id`.

### Three Levels of Storage

| Type | Description | Use Case |
|------|-------------|----------|
| memory | Stored only in memory, invalidated when the app closes | UI state, temporary variables |
| local | Stored locally, not involved in synchronization | Cache, platform settings, third-party API return data |
| sync | Stored and automatically synced to the cloud | User data, multi-device consistency |

#### Q: Can I just use memory level storage?
Yes, you can use memory level storage. If you use it that way, it can be used as a ordinary state management tool. And the answer can be used to answer another question of Can I just use memroy and local level storage.

### Schemas

<!---->
<!-- # APIs -->
<!---->
<!-- - Initialization -->
<!-- - Define Schemas -->
<!-- - Get and Set and Remove -->
<!-- - Get State -->
<!---->
<!---->
<!-- ## Initialization -->
<!-- In order to use the amazing feature this package provides, you need first to initialize it with two parameters: -->
<!-- - `api_key`: The API key you received from the [Fancy Storage](https://github.com/gcanoxl/fancy-storage), representing your app's identity. -->
<!-- - `sync_id`: The id your app creates for different users, representing each user's identity. -->
<!---->
<!-- In a word, a project should have an `api_key` but multiple `sync_id` for different users. -->
<!---->
<!-- `sync_id` can be generated by yourself or using `uuid` to let the package generate it for you. -->
<!---->
<!-- ```dart -->
<!-- void main() async { -->
<!--     await WidgetsFlutterBinding.ensureInitialized(); -->
<!--     await FancyStorage.initialize( -->
<!--         api_key: "YOUR_API_KEY", -->
<!--         sync_id: "uuid", // or "YOUR_SYNC_ID" -->
<!--     ); -->
<!-- } -->
<!-- ``` -->
<!---->
<!-- ## Usage -->
<!-- In order to use FancyStorage, the first thing you need to do is to get a reference to it. If you are a flutter developer, you can use `FancyStorage.of(context)` to do so across the whole widget tree. -->
<!---->
<!-- ```dart -->
<!-- FancyStorage.of(context).put<T>("key", object<as T>); -->
<!-- FancyStorage.of(context).get<T>("key", {defaultValue: defaultValue<as T>, notWatch: false}); -->
<!-- FancyStorage.of(context).remove("key"); -->
<!-- ``` -->
<!---->
<!-- ### Use Key-Value Pair as List -->
<!---->
<!-- ```dart -->
<!-- FancyStorage.of(context).putAsList<T>("key", ["value1", "value2"]); -->
<!-- FancyStorage.of(context).getAsList<T>("key", {filter: (T value, int index) => true}, {defaultValue: defaultValue<as List<T>>, notWatch: false}); -->
<!-- FancyStorage.of(context).remove("key"); -->
<!-- FancyStorage.of(context).operateOnList<T>("key", (T value, int index) => T); -->
<!-- ``` -->
<!---->
<!-- **operateOnList** -->
<!---->
<!-- ```dart  -->
<!-- FancyStorage.of(context).operateOnList<String>("todo_list", (value, index) { -->
<!--     return value.toUpperCase(); // just an example -->
<!--     // or -->
<!--     return null; // delete it. the index will not be changed until the whole iteration is done. -->
<!-- }); -->
<!-- ``` -->
<!---->
<!-- ### Usage with other state management packages -->
<!---->
<!-- ```dart  -->
<!-- await FancyStorageCore.put<T>("key", value); -->
<!-- final value = await FancyStorageCore.get<T>("key"); -->
<!-- ``` -->
<!---->
<!-- ## Example  -->
<!-- ```dart -->
<!-- class MyApp extends StatelessWidget { -->
<!--   @override -->
<!--   Widget build(BuildContext context) { -->
<!--     return MaterialApp( -->
<!--       home: Builder(builder: (context) { -->
<!--         final storage = FancyStorage.of(context); -->
<!--         return Scaffold( -->
<!--           appBar: AppBar(title: Text("Hello ${storage.get<String>('name' defaultValue: 'User')}")), -->
<!--           body: Center( -->
<!--             child: ElevatedButton( -->
<!--               onPressed: () { -->
<!--                 storage.put<String>("name", "Peter"); -->
<!--               }, -->
<!--               child: Text("Save Greeting"), -->
<!--             ), -->
<!--           ), -->
<!--         ); -->
<!--       }), -->
<!--     ); -->
<!--   } -->
<!-- } -->
<!-- ``` -->
